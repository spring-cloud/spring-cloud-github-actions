{
  "name": "update-antora-playbook",
  "version": "1.0.0",
  "description": "GitHub Action to update an Antora playbook when a new commercial branch is created",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@actions/core": "1.11.1",
    "@actions/exec": "1.1.1",
    "picomatch": "2.3.2",
    "yaml": "2.7.1"
  },
  "devDependencies": {
    "@vercel/ncc": "0.38.3",
    "jest": "29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  }
}
