<?xml version="1.0" encoding="UTF-8"?>
<!--
  Mirrors the pre-release versions that a location-by-location check misses.
  Modelled on spring-cloud-function-samples/function-sample-gcp-background/pom.xml,
  where a -SNAPSHOT pinned on a plugin's own <dependencies> survived a release.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>io.spring.sample</groupId>
  <artifactId>function-sample-gcp-background</artifactId>
  <version>0.0.1-SNAPSHOT</version><!-- @releaser:version-check-off -->
  <packaging>jar</packaging>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.7</version>
    <relativePath/>
  </parent>

  <properties>
    <java.version>17</java.version>
    <spring-cloud-function.version>5.0.4</spring-cloud-function.version>
    <!-- kebab-case property name: not matched by a `.version` suffix check -->
    <spring-cloud-commons-version>4.3.0-SNAPSHOT</spring-cloud-commons-version>
    <!-- no version-looking key at all -->
    <internal.tooling>1.2.0-RC1</internal.tooling>
    <!-- a URL that happens to end in -snapshot must not be read as a version -->
    <repo.url>https://repo.spring.io/libs-snapshot</repo.url>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-function-adapter-gcp</artifactId>
      <version>${spring-cloud-function.version}</version>
    </dependency>
  </dependencies>

  <build>
    <extensions>
      <extension>
        <groupId>org.example</groupId>
        <artifactId>my-wagon</artifactId>
        <version>1.0.0-SNAPSHOT</version>
      </extension>
    </extensions>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <outputDirectory>target/deploy</outputDirectory>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-function-adapter-gcp</artifactId>
            <version>3.1.0-SNAPSHOT</version>
          </dependency>
        </dependencies>
      </plugin>
    </plugins>
  </build>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.6.0-M1</version>
      </plugin>
    </plugins>
  </reporting>

  <profiles>
    <profile>
      <id>spring</id>
      <properties>
        <spring-boot.version>3.5.0-SNAPSHOT</spring-boot.version>
      </properties>
      <dependencies>
        <dependency>
          <groupId>org.example</groupId>
          <artifactId>profile-only-dep</artifactId>
          <version>9.9.9-SNAPSHOT</version>
        </dependency>
      </dependencies>
      <repositories>
        <repository>
          <id>spring-snapshots</id>
          <name>Spring Snapshots</name>
          <url>https://repo.spring.io/libs-snapshot-local</url>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </repository>
      </repositories>
    </profile>
  </profiles>
</project>
